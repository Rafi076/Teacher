
import 'selected_date_widget.dart' show SelectedDateWidget;
import 'package:flutter/material.dart';
import '/flutter_flow/flutter_flow_model.dart'; // Ensure this import path is correct based on your project structure


class SelectedDateModel extends FlutterFlowModel<SelectedDateWidget> {
  ///  State fields for stateful widgets in this page.

  final unfocusNode = FocusNode();

  @override
  void initState(BuildContext context) {
    super.initState(context);
  }

  @override
  void dispose() {
    super.dispose();
  }
}
